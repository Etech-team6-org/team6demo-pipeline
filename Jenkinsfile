pipeline {
    agent any

    stages {
        stage('Kingsley') {
            steps {
                sh 'whoami'
                sh 'sudo systemctl status jenkins'
            }
        }

        stage('Oby') {
            steps {
                sh 'pwd'
                sh 'sudo systemctl status jenkins'
            }
        }

        stage('Matthew') {
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
            }
        }
        
    }
}
